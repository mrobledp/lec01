name: Prueba de GitHub Actions

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout del código
        uses: actions/checkout@v3
      - name: Ejecutar un mensaje
        run: echo "¡Hola desde GitHub Actions!"
